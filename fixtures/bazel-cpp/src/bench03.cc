// Generated by scripts/gen_bazel_fixture.py. Do not edit.
#include <cstdint>
#include <cstdio>
#include <vector>

#include "src/lib211.h"
#include "src/lib221.h"
#include "src/lib254.h"
#include "src/lib257.h"
#include "src/lib268.h"
#include "src/lib274.h"

int main(int argc, char** argv) {
  std::uint64_t acc = static_cast<std::uint64_t>(argc);
  std::vector<std::uint64_t> input;
  for (int i = 0; i < 64; ++i) {
    input.push_back(static_cast<std::uint64_t>(i) * 2654435761ULL);
  }

  acc ^= kilncache_bench::lib211_checksum(acc + 211);
  acc ^= kilncache_bench::lib221_checksum(acc + 221);
  acc ^= kilncache_bench::lib254_checksum(acc + 254);
  acc ^= kilncache_bench::lib257_checksum(acc + 257);
  acc ^= kilncache_bench::lib268_checksum(acc + 268);
  acc ^= kilncache_bench::lib274_checksum(acc + 274);

  std::printf("bench03 checksum=%llu\n", static_cast<unsigned long long>(acc));
  (void)argv;
  return 0;
}
