// Generated by scripts/gen_bazel_fixture.py. Do not edit.
#include <cstdint>
#include <cstdio>
#include <vector>

#include "src/lib216.h"
#include "src/lib221.h"
#include "src/lib224.h"
#include "src/lib256.h"
#include "src/lib263.h"
#include "src/lib276.h"

int main(int argc, char** argv) {
  std::uint64_t acc = static_cast<std::uint64_t>(argc);
  std::vector<std::uint64_t> input;
  for (int i = 0; i < 64; ++i) {
    input.push_back(static_cast<std::uint64_t>(i) * 2654435761ULL);
  }

  acc ^= kilncache_bench::lib216_checksum(acc + 216);
  acc ^= kilncache_bench::lib221_checksum(acc + 221);
  acc ^= kilncache_bench::lib224_checksum(acc + 224);
  acc ^= kilncache_bench::lib256_checksum(acc + 256);
  acc ^= kilncache_bench::lib263_checksum(acc + 263);
  acc ^= kilncache_bench::lib276_checksum(acc + 276);

  std::printf("bench18 checksum=%llu\n", static_cast<unsigned long long>(acc));
  (void)argv;
  return 0;
}
