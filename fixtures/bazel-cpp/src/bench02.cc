// Generated by scripts/gen_bazel_fixture.py. Do not edit.
#include <cstdint>
#include <cstdio>
#include <vector>

#include "src/lib213.h"
#include "src/lib232.h"
#include "src/lib249.h"
#include "src/lib277.h"

int main(int argc, char** argv) {
  std::uint64_t acc = static_cast<std::uint64_t>(argc);
  std::vector<std::uint64_t> input;
  for (int i = 0; i < 64; ++i) {
    input.push_back(static_cast<std::uint64_t>(i) * 2654435761ULL);
  }

  acc ^= kilncache_bench::lib213_checksum(acc + 213);
  acc ^= kilncache_bench::lib232_checksum(acc + 232);
  acc ^= kilncache_bench::lib249_checksum(acc + 249);
  acc ^= kilncache_bench::lib277_checksum(acc + 277);

  std::printf("bench02 checksum=%llu\n", static_cast<unsigned long long>(acc));
  (void)argv;
  return 0;
}
