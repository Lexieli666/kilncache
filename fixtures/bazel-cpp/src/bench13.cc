// Generated by scripts/gen_bazel_fixture.py. Do not edit.
#include <cstdint>
#include <cstdio>
#include <vector>

#include "src/lib234.h"
#include "src/lib263.h"
#include "src/lib271.h"
#include "src/lib277.h"

int main(int argc, char** argv) {
  std::uint64_t acc = static_cast<std::uint64_t>(argc);
  std::vector<std::uint64_t> input;
  for (int i = 0; i < 64; ++i) {
    input.push_back(static_cast<std::uint64_t>(i) * 2654435761ULL);
  }

  acc ^= kilncache_bench::lib234_checksum(acc + 234);
  acc ^= kilncache_bench::lib263_checksum(acc + 263);
  acc ^= kilncache_bench::lib271_checksum(acc + 271);
  acc ^= kilncache_bench::lib277_checksum(acc + 277);

  std::printf("bench13 checksum=%llu\n", static_cast<unsigned long long>(acc));
  (void)argv;
  return 0;
}
