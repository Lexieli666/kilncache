// Generated by scripts/gen_bazel_fixture.py. Do not edit.
#include <cstdint>
#include <cstdio>
#include <vector>

#include "src/lib211.h"
#include "src/lib235.h"
#include "src/lib248.h"
#include "src/lib259.h"
#include "src/lib274.h"
#include "src/lib276.h"

int main(int argc, char** argv) {
  std::uint64_t acc = static_cast<std::uint64_t>(argc);
  std::vector<std::uint64_t> input;
  for (int i = 0; i < 64; ++i) {
    input.push_back(static_cast<std::uint64_t>(i) * 2654435761ULL);
  }

  acc ^= kilncache_bench::lib211_checksum(acc + 211);
  acc ^= kilncache_bench::lib235_checksum(acc + 235);
  acc ^= kilncache_bench::lib248_checksum(acc + 248);
  acc ^= kilncache_bench::lib259_checksum(acc + 259);
  acc ^= kilncache_bench::lib274_checksum(acc + 274);
  acc ^= kilncache_bench::lib276_checksum(acc + 276);

  std::printf("bench08 checksum=%llu\n", static_cast<unsigned long long>(acc));
  (void)argv;
  return 0;
}
