// Generated by scripts/gen_bazel_fixture.py. Do not edit.
#include <cstdint>
#include <cstdio>
#include <vector>

#include "src/lib223.h"
#include "src/lib224.h"
#include "src/lib238.h"
#include "src/lib241.h"
#include "src/lib247.h"
#include "src/lib277.h"

int main(int argc, char** argv) {
  std::uint64_t acc = static_cast<std::uint64_t>(argc);
  std::vector<std::uint64_t> input;
  for (int i = 0; i < 64; ++i) {
    input.push_back(static_cast<std::uint64_t>(i) * 2654435761ULL);
  }

  acc ^= kilncache_bench::lib223_checksum(acc + 223);
  acc ^= kilncache_bench::lib224_checksum(acc + 224);
  acc ^= kilncache_bench::lib238_checksum(acc + 238);
  acc ^= kilncache_bench::lib241_checksum(acc + 241);
  acc ^= kilncache_bench::lib247_checksum(acc + 247);
  acc ^= kilncache_bench::lib277_checksum(acc + 277);

  std::printf("bench10 checksum=%llu\n", static_cast<unsigned long long>(acc));
  (void)argv;
  return 0;
}
