// Generated by scripts/gen_bazel_fixture.py. Do not edit.
#include <cstdint>
#include <cstdio>
#include <vector>

#include "src/lib212.h"
#include "src/lib214.h"
#include "src/lib215.h"
#include "src/lib244.h"
#include "src/lib249.h"
#include "src/lib268.h"

int main(int argc, char** argv) {
  std::uint64_t acc = static_cast<std::uint64_t>(argc);
  std::vector<std::uint64_t> input;
  for (int i = 0; i < 64; ++i) {
    input.push_back(static_cast<std::uint64_t>(i) * 2654435761ULL);
  }

  acc ^= kilncache_bench::lib212_checksum(acc + 212);
  acc ^= kilncache_bench::lib214_checksum(acc + 214);
  acc ^= kilncache_bench::lib215_checksum(acc + 215);
  acc ^= kilncache_bench::lib244_checksum(acc + 244);
  acc ^= kilncache_bench::lib249_checksum(acc + 249);
  acc ^= kilncache_bench::lib268_checksum(acc + 268);

  std::printf("bench06 checksum=%llu\n", static_cast<unsigned long long>(acc));
  (void)argv;
  return 0;
}
