// Generated by scripts/gen_bazel_fixture.py. Do not edit.
#include <cstdint>
#include <cstdio>
#include <vector>

#include "src/lib212.h"
#include "src/lib243.h"
#include "src/lib254.h"
#include "src/lib260.h"
#include "src/lib264.h"

int main(int argc, char** argv) {
  std::uint64_t acc = static_cast<std::uint64_t>(argc);
  std::vector<std::uint64_t> input;
  for (int i = 0; i < 64; ++i) {
    input.push_back(static_cast<std::uint64_t>(i) * 2654435761ULL);
  }

  acc ^= kilncache_bench::lib212_checksum(acc + 212);
  acc ^= kilncache_bench::lib243_checksum(acc + 243);
  acc ^= kilncache_bench::lib254_checksum(acc + 254);
  acc ^= kilncache_bench::lib260_checksum(acc + 260);
  acc ^= kilncache_bench::lib264_checksum(acc + 264);

  std::printf("bench04 checksum=%llu\n", static_cast<unsigned long long>(acc));
  (void)argv;
  return 0;
}
