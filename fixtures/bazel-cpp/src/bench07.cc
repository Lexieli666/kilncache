// Generated by scripts/gen_bazel_fixture.py. Do not edit.
#include <cstdint>
#include <cstdio>
#include <vector>

#include "src/lib214.h"
#include "src/lib238.h"
#include "src/lib239.h"
#include "src/lib257.h"
#include "src/lib258.h"
#include "src/lib273.h"

int main(int argc, char** argv) {
  std::uint64_t acc = static_cast<std::uint64_t>(argc);
  std::vector<std::uint64_t> input;
  for (int i = 0; i < 64; ++i) {
    input.push_back(static_cast<std::uint64_t>(i) * 2654435761ULL);
  }

  acc ^= kilncache_bench::lib214_checksum(acc + 214);
  acc ^= kilncache_bench::lib238_checksum(acc + 238);
  acc ^= kilncache_bench::lib239_checksum(acc + 239);
  acc ^= kilncache_bench::lib257_checksum(acc + 257);
  acc ^= kilncache_bench::lib258_checksum(acc + 258);
  acc ^= kilncache_bench::lib273_checksum(acc + 273);

  std::printf("bench07 checksum=%llu\n", static_cast<unsigned long long>(acc));
  (void)argv;
  return 0;
}
