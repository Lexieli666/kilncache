// Generated by scripts/gen_bazel_fixture.py. Do not edit.
#include <cstdint>
#include <cstdio>
#include <vector>

#include "src/lib220.h"
#include "src/lib222.h"
#include "src/lib269.h"
#include "src/lib270.h"
#include "src/lib275.h"

int main(int argc, char** argv) {
  std::uint64_t acc = static_cast<std::uint64_t>(argc);
  std::vector<std::uint64_t> input;
  for (int i = 0; i < 64; ++i) {
    input.push_back(static_cast<std::uint64_t>(i) * 2654435761ULL);
  }

  acc ^= kilncache_bench::lib220_checksum(acc + 220);
  acc ^= kilncache_bench::lib222_checksum(acc + 222);
  acc ^= kilncache_bench::lib269_checksum(acc + 269);
  acc ^= kilncache_bench::lib270_checksum(acc + 270);
  acc ^= kilncache_bench::lib275_checksum(acc + 275);

  std::printf("bench12 checksum=%llu\n", static_cast<unsigned long long>(acc));
  (void)argv;
  return 0;
}
