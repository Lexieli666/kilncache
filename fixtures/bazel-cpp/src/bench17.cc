// Generated by scripts/gen_bazel_fixture.py. Do not edit.
#include <cstdint>
#include <cstdio>
#include <vector>

#include "src/lib223.h"
#include "src/lib245.h"
#include "src/lib261.h"
#include "src/lib263.h"
#include "src/lib267.h"

int main(int argc, char** argv) {
  std::uint64_t acc = static_cast<std::uint64_t>(argc);
  std::vector<std::uint64_t> input;
  for (int i = 0; i < 64; ++i) {
    input.push_back(static_cast<std::uint64_t>(i) * 2654435761ULL);
  }

  acc ^= kilncache_bench::lib223_checksum(acc + 223);
  acc ^= kilncache_bench::lib245_checksum(acc + 245);
  acc ^= kilncache_bench::lib261_checksum(acc + 261);
  acc ^= kilncache_bench::lib263_checksum(acc + 263);
  acc ^= kilncache_bench::lib267_checksum(acc + 267);

  std::printf("bench17 checksum=%llu\n", static_cast<unsigned long long>(acc));
  (void)argv;
  return 0;
}
