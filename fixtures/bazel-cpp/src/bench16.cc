// Generated by scripts/gen_bazel_fixture.py. Do not edit.
#include <cstdint>
#include <cstdio>
#include <vector>

#include "src/lib223.h"
#include "src/lib226.h"
#include "src/lib266.h"
#include "src/lib272.h"

int main(int argc, char** argv) {
  std::uint64_t acc = static_cast<std::uint64_t>(argc);
  std::vector<std::uint64_t> input;
  for (int i = 0; i < 64; ++i) {
    input.push_back(static_cast<std::uint64_t>(i) * 2654435761ULL);
  }

  acc ^= kilncache_bench::lib223_checksum(acc + 223);
  acc ^= kilncache_bench::lib226_checksum(acc + 226);
  acc ^= kilncache_bench::lib266_checksum(acc + 266);
  acc ^= kilncache_bench::lib272_checksum(acc + 272);

  std::printf("bench16 checksum=%llu\n", static_cast<unsigned long long>(acc));
  (void)argv;
  return 0;
}
